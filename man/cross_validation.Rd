% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validation.R
\name{cross_validation}
\alias{cross_validation}
\title{Perform k-fold cross-validation}
\usage{
cross_validation(model, data, k = 5, seed = 123)
}
\arguments{
\item{model}{A model object (e.g., lm, glm)}

\item{data}{A data frame containing the dataset}

\item{k}{An integer representing the number of folds (default is 5)}

\item{seed}{An integer seed for reproducibility (default is 123)}
}
\value{
A list with the following components:
\item{predictions}{A list of numeric vectors, where each vector contains the predictions for one fold}
}
\description{
This function performs k-fold cross-validation on a given model and dataset.
A seed is used to ensure reproducibility.
}
\examples{
data(mtcars)

model <- lm(mpg ~ ., data = mtcars)
cv_results <- cross_validation(model, mtcars, k = 5)

head(cv_results$predictions)
}
